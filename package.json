{
  "name": "unlvjazz-site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "node --experimental-strip-types scripts/test-events.ts",
    "check": "node --experimental-strip-types scripts/check-content.ts && node --experimental-strip-types scripts/check-contrast.ts"
  },
  "dependencies": {
    "astro": "^7.3.1"
  },
  "allowScripts": {
    "esbuild": true
  }
}
